:host {
  display: inline-flex;
  width: 600rpx;
  height: 300rpx;
}
.fd-canvas {
  position: relative;
  display: inline-flex;
}
.fd-canvas,
.canvas {
  width: inherit;
  height: inherit;
}
.cut,
.clip,
.rotate {
  position: absolute;
  left: 0;
  right: 0;
  transform: translateY(-1000%);
}
